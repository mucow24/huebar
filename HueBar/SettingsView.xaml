<UserControl x:Class="HueBar.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}"
             FontFamily="Segoe UI Variable Text, Segoe UI"
             FontSize="14"
             TextOptions.TextFormattingMode="Ideal"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">

    <!-- Colour brushes are NOT declared here: they're injected into this dictionary at runtime by
         ApplyTheme() from the HueBar.Core ThemePalette (light or dark, per the OS preference), which
         is why every colour below is a DynamicResource. Only the theme-invariant styles live here. -->
    <UserControl.Resources>
        <!-- Secondary (neutral) button -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="{DynamicResource ControlFillBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Height" Value="34" />
            <Setter Property="MinWidth" Value="84" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource ControlHoverFillBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource ControlPressedFillBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary (accent) button -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
            <Setter Property="Foreground" Value="{DynamicResource OnAccentTextBrush}" />
            <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource AccentHoverBrush}" />
                                <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource AccentHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource AccentPressedBrush}" />
                                <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource AccentPressedBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Subtle, borderless text button (Forget). Accent-coloured on hover. -->
        <Style x:Key="SubtleButton" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource SubtleTextBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Bridge-row radio: an accent dot + the row content; clicking anywhere selects it. -->
        <Style x:Key="BridgeRadio" TargetType="RadioButton">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="Transparent">
                            <StackPanel Orientation="Horizontal">
                                <Grid Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,11,0">
                                    <Ellipse x:Name="ring" Width="18" Height="18"
                                             Stroke="{DynamicResource ControlBorderBrush}" StrokeThickness="1.5" />
                                    <Ellipse x:Name="dot" Width="9" Height="9"
                                             Fill="{DynamicResource AccentBrush}" Visibility="Collapsed" />
                                </Grid>
                                <ContentPresenter VerticalAlignment="Center" />
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="ring" Property="Stroke" Value="{DynamicResource AccentBrush}" />
                                <Setter TargetName="dot" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Flat, rounded text box -->
        <Style x:Key="FlatTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="8,0" />
            <Setter Property="Background" Value="{DynamicResource TextBoxBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Section header (small, subtle, all-caps-ish spacing) -->
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource SubtleTextBrush}" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
    </UserControl.Resources>

    <!-- Fixed width lives on this inner Border (not the UserControl root, whose Width ElementHost
         resets to auto), so the layout has a stable width and long status text wraps instead of
         stretching the window. -->
    <Border Width="480" Padding="22,20,22,20">
    <StackPanel>
        <TextBlock Text="HueBar settings"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource TextBrush}"
                   Margin="0,0,0,18" />

        <!-- Bridges -->
        <TextBlock Text="Bridges" Style="{StaticResource SectionHeader}" />
        <Border BorderBrush="{DynamicResource ControlBorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="6">
            <StackPanel>
                <TextBlock x:Name="_emptyBridges"
                           Text="No bridges yet. Add one below."
                           Foreground="{DynamicResource SubtleTextBrush}"
                           Margin="6,8,6,8"
                           Visibility="Collapsed" />
                <StackPanel x:Name="_bridgeList" />
            </StackPanel>
        </Border>

        <TextBlock Text="Switching is instant — HueBar already has each bridge's key, so there's nothing to press."
                   Foreground="{DynamicResource SubtleTextBrush}"
                   FontSize="12"
                   TextWrapping="Wrap"
                   Margin="2,8,0,0" />

        <!-- Add a bridge (the old connect flow) -->
        <TextBlock Text="Add a bridge" Style="{StaticResource SectionHeader}" Margin="0,20,0,8" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button x:Name="_discoverButton"
                    Grid.Column="0"
                    Content="Discover"
                    Style="{StaticResource SecondaryButton}"
                    Click="OnDiscover" />

            <TextBlock Grid.Column="1"
                       Text="Bridge IP:"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource TextBrush}"
                       Margin="14,0,8,0" />

            <TextBox x:Name="_ipBox"
                     Grid.Column="2"
                     Style="{StaticResource FlatTextBox}" />

            <Button x:Name="_connectButton"
                    Grid.Column="3"
                    Content="Connect"
                    IsDefault="True"
                    Style="{StaticResource PrimaryButton}"
                    Margin="10,0,0,0"
                    Click="OnConnect" />
        </Grid>

        <TextBlock x:Name="_statusLabel"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource SubtleTextBrush}"
                   Margin="0,12,0,0"
                   MinHeight="19" />

        <!-- General -->
        <TextBlock Text="General" Style="{StaticResource SectionHeader}" Margin="0,20,0,8" />
        <CheckBox x:Name="_includeZones"
                  Content="Include zones, not just rooms, in the menu"
                  Foreground="{DynamicResource TextBrush}"
                  Click="OnIncludeZonesChanged" />
    </StackPanel>
    </Border>
</UserControl>
